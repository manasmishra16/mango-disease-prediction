# Project Rules

- **Python Tooling**: Use `uv` for all Python-related operations (package management, environment creation, running scripts).
- **Directory Structure**:
    - Do not litter the root directory.
    - Keep all logic, data, and assets organized within their respective subdirectories (`src/`, `backend/`, `frontend/`, `data/`, etc.).
    - Root should only contain essential configuration files.
